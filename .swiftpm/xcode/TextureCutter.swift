import SpriteKit

enum TextureCutter {
    // Returns a new texture after applying a cut along the provided points.
    // This is a stub implementation that simply returns the original texture
    // so the project compiles. Replace with real texture cutting logic later.
    static func cutTexture(sourceTexture: SKTexture, points: [CGPoint]) -> SKTexture? {
        return sourceTexture
    }
}
